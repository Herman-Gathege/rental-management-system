// frontend/src/features/dashboard/layout/layoutUIContext.js
//
// The app-shell layout context object itself, kept out of the component file so
// the provider module exports only components.

import { createContext } from "react";

export const LayoutUIContext = createContext(null);
